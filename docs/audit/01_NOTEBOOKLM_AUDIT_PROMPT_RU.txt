АУДИТ-ПРОМПТ (RU) — Collabka PR (@collabkaprbot)

Роль: ты — независимый senior/audit engineer (Backend + SRE + Security + Product).
Цель: найти уязвимости, регресс‑риски, UX‑тупики и ошибки в admin/broadcast/monetization/cron.
Ограничение: отвечай ТОЛЬКО фактами из загруженных файлов. Если данных нет — так и пиши.

Жёсткие инварианты проекта (нельзя предлагать решения, которые их ломают):
1) Vercel/serverless: никаких долгих процессов; всё пакетно; таймауты.
2) Neon дорогой: не добавляй новые DB‑reads в горячих UI путях (меню/хабы/рендер).
3) Монетизация/разлок/списания — только DB‑truth и атомарные guards; никаких «поверим Redis».
4) Redis может деградировать: мутации fail‑closed, навигация fail‑open по allowlist.
5) Telegram callback_data ≤ 64 байта.

ФОРМАТ ОТЧЁТА (строго):

0) Входные данные
- Перечисли ключевые файлы/папки, на которые ты опирался (paths).
- Если есть STEP/версия в доках — укажи её.

1) P0: Blockers (если есть)
- Любая причина «бот не стартует после деплоя» или «endpoint падает».
- Для каждого: доказательство (path + фрагмент ≤25 слов + search‑phrase) → как воспроизвести → минимальный фикс.

2) Карта входов (коротко)
- /start payload префиксы
- основные callback actions (a:*)
- api/cron endpoints
- support/admin entrypoints
(Не надо простыню: 20–40 строк максимум, но покрыть всё ядро.)

3) Top‑15 проблем (P0–P2)
Для каждого пункта ОБЯЗАТЕЛЬНО:
- Severity: CRITICAL/HIGH/MED/LOW
- Где: path + функция/handler + search‑phrase
- Repro steps (конкретно)
- Impact (что ломается/утекает/сколько денег/какой актор)
- Fix (минимальный, обратимый; не более 1–3 изменения)
- Риск фикса (Low/Med/High)

4) Мини‑патчи (макс 5)
- Список 3–5 самых выгодных фиксов «малой кровью».
- Каждый фикс: какие файлы трогаем + почему это не добавляет DB‑reads в hot UI.

ЧЕК‑ЛИСТ: что ты обязан проверить в коде (не пропускай)
A) Official publish (в @collabka_offers)
- reserve/lock/idempotency: не должно быть дублей при ретраях/таймаутах.
- сценарий «TG send успешен, DB финализация упала» — что происходит.

B) Broadcast
- 429 rate‑limit: Redis cooldown + уважение паузы воркером.
- /api/health показывает паузу (Redis‑only).
- URL‑кнопки (до 3), HTML entities → корректный рендер.

C) Brand Inbox (точка списания)
- До «✅ Принять» доступны только «Принять/Спам/Удалить».
- Никаких reply/templates/work/close при status=new (даже через старые кнопки).
- Списание кредитов exactly‑once на new→in_progress (атомарно), без минуса.

D) Payments/credits
- pre_checkout/successful_payment: идемпотентность apply, повторные апдейты не должны списывать/открывать дважды.

E) Contacts / anti‑bypass
- редактирование/шаблоны/IG verify: нельзя протащить @username/ссылки до unlock.
- structured контакты (JSONB) vs legacy text: приоритет и redaction.

F) Redis degraded mode
- мутации fail‑closed (никаких «✅ сделано», если Redis/DB не подтвердили).
- навигация fail‑open (кнопки меню/home), без петли «всё недоступно».

G) Migrations
- runner exactly‑once, checksum normalization, pack синхронизирован.

Важно про доказательства:
- Каждое утверждение — с указанием пути файла и коротким фрагментом ≤25 слов.
- Если line numbers недоступны — добавь search‑phrase (уникальная строка для grep).

Выход должен быть практичным: без философии, без общих слов, только проверяемые находки.
