АУДИТ-ПРОМПТ (RU) — Collabka PR (@collabkaprbot) — baseline STEP430 (после STEP430 refresh)

Роль: ты — независимый senior/audit engineer (Backend + SRE + Security + Product).

Вход: ты получил NotebookLM pack (≤50 txt файлов), собранный из репозитория Collabka PR. Отвечай ТОЛЬКО фактами из загруженных файлов. Если данных нет — так и пиши.

Цель: дать честный вердикт GO/NO-GO для продакшена СЕЙЧАС и, если нужно, предложить 1–3 минимальных, обратимых микро-фикса без расширения поверхности.

Жёсткие инварианты проекта (нельзя предлагать решения, которые их ломают):
1) Serverless (Vercel): пакетная обработка, таймауты; никаких бесконечных циклов.
2) Neon Postgres бережём: нельзя добавлять лишние DB-read в горячие UI пути (меню/рендер/хабы), если это не критично и не замерено.
3) Монетизация/списания/разлок — только DB-truth + атомарные guards (exactly-once).
4) Redis может деградировать: mutating callbacks fail-closed; навигация максимально fail-open на allowlist.
5) Broadcast/cron: lock/throttle; при overload — 429 + Retry-After + jitter; не допускать thundering herd.
6) Миграции: только через `migrations/run.js` (exactly-once + checksum). Winners: детерминированно.

---

## 1) Вердикт (строго)
Дай вердикт в первой строке: **GO** или **NO-GO**.

Опирайся на `/api/health`:
- `system_status` и `no_go_reasons[]` (включая `hint`)
- `redis.read_ok/write_ok`
- `payments.payload_hmac_key_configured` + `payments.payload_hmac_minlen_ok`
- `payments.fallback_apply_effective`
- counters: `broadcast.db_overload.today_count`, `broadcast.tick_deferred_redis.today_count`,
  `qstash.reschedule_failed.today_count`, `qstash.official_publish_stuck.today_count`
- observability: `ops.digest_preview`
- early-stuck: `broadcast.pending_deliveries`

Если **NO-GO** — перечисли 1–5 конкретных `no_go_reasons` (code/value/threshold/hint), без воды.

---

## 2) PASS/FAIL по критическим контурам
Сделай таблицу PASS/FAIL (коротко, по делу) минимум по 8 контурам:
1) Payments (HMAC/amount validation, apply exactly-once, fallback baseline OFF + runtime control)
2) Giveaways (детерминизм/SQL, транзакционные guards)
3) Broadcast deliver (429 + Retry-After + jitter + fuse до DB)
4) Broadcast tick (defer при Redis-down, lock/throttle)
5) RateLimit (circuit breaker, degraded режим строже)
6) Official publish (token-lock + verify + self-heal, idempotency)
7) /api/health + Admin→Ops (never-throw, system_status/no_go_reasons, digest_preview, ops controls)
8) Contacts unlock + redaction (anti-bypass, “не списывает за пустоту”)

Плюсом (если данные есть) оцени:
- Hard-skip list + HITs report (видимость причин/экспорт)
- Staging fault-injection (`SIMULATE_REDIS_DOWN`) и smoke scripts (проверка fail-open/fail-closed)
- Preflight coverage (node --check entrypoints + smoke tests)
- Creator current-channel / share URL / Brand Inbox / contacts unlock / no-channel gates / input-mode / what-next-backnav contract smokes

---

## 3) Readiness checklist (операторский, 30 минут)
Составь короткий чеклист запуска:
- `node migrations/run.js`
- `/api/health` должен быть зелёный (GO) и counters в норме
- Пройти one-screen check по `docs/ops/02_HEALTH_ONE_SCREEN.md`
- В Admin→Ops нет красных баннеров; если нужно — `Flush ops digest`
- Baseline ENV: HMAC key configured/≥32 bytes, fallback apply baseline OFF, IG UI скрыт/не блокирует прод
- Мини-smoke: креатор+бренд, один unlock, один небольшой broadcast, один giveaway

---

## 4) Если нужно “добить до 10/10” без риска
Предложи 1–3 микро-шага (в стиле STEP381+), каждый:
- очень узкий, обратимый
- не добавляет DB-reads в hot UI
- содержит “что поменять” (файлы) и “как проверить” (QA)

В конце: список 3–7 главных регресс-рисков (где чаще ломают прод).
