# Install STEP590E5C

Baseline: operator-pushed STEP590E5B commit `fe20572`.

1. Extract the STEP590E5C HOTFIX into `C:\GitHub\collabkaprbot` with overwrite enabled.
2. Do not apply the PATCH in addition to the HOTFIX.
3. Run the commands from `docs/operations/STEP590E5C_ADMIN_COMMUNICATIONS_NOTICES_OUTBOX_ROLLOUT_RUNBOOK.md`.
4. Expected package version: `1.3.29`.
5. Expected ownership: `433 extracted / 127 legacy / 7 aliases / 0 unresolved`.
6. Commit only after all operator-side gates pass.
