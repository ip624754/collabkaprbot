# Install STEP590E5B

1. Confirm the current repository includes STEP590E5A commit `acbe16d` and has a clean worktree.
2. Extract the STEP590E5B HOTFIX into the repository root with overwrite.
3. Run the commands in `STEP590E5B_QA_checklist.md`.
4. Commit only after all local gates pass.
5. No migration or ENV update is required.
