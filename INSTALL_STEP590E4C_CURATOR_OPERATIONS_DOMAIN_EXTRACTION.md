
# Install STEP590E4C

Baseline: STEP590E4B commit `e738784`.

1. Confirm branch `main` and clean worktree.
2. Extract `collabkaprbot_STEP590E4C_CURATOR_OPERATIONS_HOTFIX.zip` directly into `C:\GitHub\collabkaprbot` with overwrite.
3. Run the commands in `STEP590E4C_QA_checklist.md`.
4. Commit only after every required local gate passes.
5. Push and confirm Vercel Production `Ready` for the new commit.

Do not apply both HOTFIX and PATCH. No migration or ENV action is required.
